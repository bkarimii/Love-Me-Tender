{
	"dev": {
		"driver": "pg",
		"user": "postgres",
		"password": "opensesame",
		"host": "localhost",
		"database": "cyf_tms",
		"port": "5432"
	},
	"production": {
		"DATABASE_URL": "postgres://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}",
		"driver": "pg",
		"host": "${DB_HOST}",
		"user": "${DB_USER}",
		"password": "${DB_PASS}",
		"database": "${DB_NAME}",
		"port": "${DB_PORT}"
	}
}
